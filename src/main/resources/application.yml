spring:
  application:
    name: verbalwala-backend

  data:
    mongodb:
      uri: ${MONGODB_URI}

server:
  port: 8081


jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

gemini:
  api:
    key: ${GEMINI_API_KEY}

  model: gemini-2.5-flash